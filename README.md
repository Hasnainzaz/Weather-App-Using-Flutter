# Weather-App-Using-Flutter
A modern, responsive **Flutter Weather Application** that displays real-time weather data using the OpenWeatherMap API. The app features **dynamic weather-based UI**, **dark mode**, and a **clean animated interface**.
